import * as postgresqlMgr from './postgresql-manager';

// postgresql
export const init = postgresqlMgr.init;
export const run = postgresqlMgr.run;

// redshift
//export const tunnel = tunnelMgr.start;
//export const run = redshiftMgr.run;
